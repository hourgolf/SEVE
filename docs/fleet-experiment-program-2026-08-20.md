# Fleet experiment program · through 2026-08-20

This packet converts the first nightly fleet queue into independently reversible work. Evidence layers remain separate. Nothing below authorizes an order, position, account, route, size, or native-manager change.

## 1. FOMC exit comparison — prepared, not promoted

`fomc-follow` is the only mature exit-leak channel whose bounded challenger clears the current paired-evidence requirements.

| Evidence | Result |
|---|---:|
| Decision cohort | 35 sessions / 36 opportunities |
| Typical best move | +30.3% |
| Typical native finish | -50% |
| Challenger | ARM +35 · KEEP TWO THIRDS |
| Paired comparison | 19 paths / 19 sessions |
| Typical lift | +19.57 points |
| Improvement frequency | 95% |
| Session-clustered interval | +11.14 to +53.55 points |

The controlled comparison keeps entry, size, route, admission, and the native control fixed. `fomc-follow` remains non-executable because an FOMC-session/manual-arm gate is not sealed. This packet does not promote it or grant order authority.

## 2. QQQ exit collection — preserve

`qqq-thrust-trail` remains observe-only with active research collection. Its entry repeatedly finds movement (+47.6% typical best move), but the native finish is -40%.

Continue pairing `ARM +20 · KEEP HALF` against the native exit. It improved 78% of 9 paths with a +42.08-point typical lift, but the interval remains extremely wide (-104.25 to +88.50). One additional pair reaches the mechanical count floor; several independent sessions may still be needed to resolve uncertainty. Do not change entry, size, or the active `qqq-thrust-trail-wd` channel from this evidence.

## 3. Retirement pauses — executed and verified

The exact two-row governed preview was applied after close:

| Channel | Execution before/after | Collection before | Collection after | History |
|---|---|---|---|---|
| momo-shape | observe-only / observe-only | active | paused | preserved |
| breakout-smart-entries-iwm | observe-only / observe-only | active | paused | preserved |

Post-write SELECT verification reports five paused collectors: `power`, `power-smart-entries`, `momo-shape`, `breakout-smart-entries-iwm`, and `breakout-alt-v3-qqq`. No active manifest, broker order, position, or historical evidence changed. Each pause can be reversed through a receipt chained to the new paused receipt.

## 4. VB exit program — channel-specific

The trend/breakout group has a shared symptom, not a shared remedy. Eleven mature channels find movement and then leak it, but most existing bounded presets do **not** beat the native exit.

| Channel | Current read | Best bounded challenger | Paired evidence | Next action |
|---|---|---|---:|---|
| vb-level-break | +15.7% best move, -2.7% finish | TP +82 | 4 paths / 2 sessions; +23.72-point lift, 50% improved | Keep shadowing; far too early to switch |
| vb-ribbon-cross-iwm | +12.7% best move, -30.0% finish | TP +11 | 2 paths / 2 sessions; +20.16-point lift, 50% improved | Keep shadowing; far too early to switch |
| vb-squeeze-break-qqq | +16.7% best move, +16.0% finish | TP +155 | 6 paired paths; +22.58-point lift, 50% improved | Preserve native; challenger is not consistent |
| vb-gap-drift family | +21.9% to +23.5% mature best move, negative finishes | Current best alternatives have negative typical lift | 62–66 paired paths for IWM/QQQ | Stop recycling those presets; study time-to-MFE and post-MFE failure |
| vb-level-break IWM/QQQ | +18.1% to +24.2% best move, -30% finish | Current best alternatives have negative typical lift | 34–59 paired paths | Derive one profit-floor hypothesis from each channel’s lower-quartile move; keep stop fixed |
| vb-macd-state IWM/QQQ | +19.6% to +22.5% best move, -30% finish | Current best alternatives have negative typical lift | 34–59 paired paths | Study first-versus-later entry and loss-after-MFE before another exit shape |
| vb-pm-trend family | +20.0% to +24.2% best move, -30% to -2.5% finish | Current best alternatives have negative typical lift | 32–105 paired paths | Measure when gains disappear; do not apply a generic trail |

The reversal VB group remains separate: its mature median best move is +15.75% and median finish is +15%. Its next questions are entry context, timing, and uniqueness—not a fleet-wide exit change.

## 5. Breakout redundancy reduction — prepared

The high-redundancy graph produces two clear families:

1. `breakout-alt-v3`, CTL, ER40 and `breakout-smart-entries`, CTL, ER40, ITM. The leading pairwise correlations are 0.87–0.99 across 8–16 comparable sessions. `breakout-alt-v3-er40` is the least-negative mature reference (-$2.96 typical session), but no member has a positive typical session. Preserve one reference collector while paired same-session deltas and uniqueness are finalized; do not preserve every clone by default.
2. QQQ Alt-V3/Smart-Entries. `breakout-alt-v3-qqq` is already paused. Keep `breakout-smart-entries-qqq` only long enough to determine whether it adds unique evidence.

The IWM decision is already cleaner: `breakout-smart-entries-iwm` is paused; active `breakout-alt-v3-iwm` remains the reference. Active `breakout`, `breakout-alt-v3-itm`, and instrument-specific roots remain unchanged because this redundancy result does not prove they are interchangeable under their current routes and exits.

## 6. Next decision gates

- FOMC: do not activate until the event/manual-arm gate and reversible shadow/native comparison are sealed.
- QQQ thrust: continue the same challenger; reassess after the count floor and uncertainty both improve.
- VB: generate one new hypothesis per channel only after time-to-MFE/post-MFE loss analysis; do not retry losing presets.
- Breakout: require paired same-session outcome deltas plus a unique-evidence check before another collector pause.
- Retirements: remain paused unless an explicit resume packet demonstrates unique value.
