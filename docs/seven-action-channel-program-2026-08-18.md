# Seven-action channel program · prepared 2026-08-18

Status: **prepared research packet; not activated**.

The nightly Decision Atlas now regenerates these actions from the same frozen
channel briefs, manager paths, trail frontier, and capacity replay. Each test
changes or compares one channel-specific variable. No item grants order,
configuration, sizing, roster, schedule, or automatic-activation authority.

| # | Channel | Prepared action | Fixed during test | Review point |
|---:|---|---|---|---|
| 1 | `vb-macd-state` | Compare current all-out +50%/-30% with the existing all-out +18%/-30% shadow; retain LOCK20/30 as another observer | Entry, 4 contracts, route, priority, collision policy | 5 sessions / 10 paired trades; stop after two materially worse sessions |
| 2 | `orb-ustop-ctl` | Score the already-live after-10:30, non-CPI/OPEX gate against reconstructed raw ORB signals | B30/A13, 4 contracts, Account 3 priority 1, same-OCC protection | 5 qualified sessions / 10 paired qualified-versus-raw opportunities |
| 3 | `qqq-thrust-trail-wd` | Shadow all-out +13%/-30% against current all-out +20%/-30% | Entry, 2 contracts, Account 3 route, one entry/session | 5 sessions / 10 paired paths |
| 4 | `vb-level-break` | Compare the first eligible entry with a shadow skip-first/next-confirmed entry | +25% target, -30% stop, 2 contracts, Account 2 route | 5 sessions / 10 comparable entry-order opportunities |
| 5 | `grind-v3`, `grind-v3-2`, `breakout`, `breakout-alt-v3-itm` | Hold current configurations so their new-era results stay attributable | Entire receipt-bound channel configuration | 5 current-era sessions / 10 trades per channel or an early-stop trigger |
| 6 | Nine named program channels | Hold each channel's current quantity; continue the channel-specific 1–6 contract replay | Current per-channel size | Only after its entry/exit test resolves and marginal size survives displacement |
| 7 | `vb-ribbon-cross-iwm` | Collect two additional current-era sessions, then force a keep-paper versus observe-only review | First-entry-only, +25%/-30%, 2 contracts, Account 2 route | Review becomes due automatically at four current-era decision sessions; posture never changes automatically |

## Evidence at preparation

- `vb-macd-state`: one current-era trade, -$124; best move +28.7%; the +18
  manager shadow modeled a 51.5-point benefit on that path.
- `orb-ustop-ctl`: the qualifying entry gate is already live. Another ORB
  configuration change would contaminate the test, so the work is scoring the
  live gate against excluded raw signals.
- `qqq-thrust-trail-wd`: one current-era trade, -$108; TP13 modeled a
  44.3-point improvement on the first paired path.
- `vb-level-break`: one current-era session with two opportunities; the first
  failed and the second reached the native target. Historical virtual evidence
  contains 18 scored paths at a +$11.88/contract typical result.
- The four unchanged controls have only one latest-era session each. Their
  combined first-session result is positive, but changing them now would erase
  attribution.
- The capacity replay supports no additional contract for any program channel.
- `vb-ribbon-cross-iwm` has two decision sessions and six historical virtual
  paths with a roughly flat +$0.30/contract typical result. Two more independent
  current-era sessions will trigger review, not automatic retirement.

## Activation boundary

Actions 1, 3, and 4 are prepared comparisons, not live configuration changes.
Action 2 is measurement of an already-live ORB entry gate. Actions 5 and 6 are
explicit holds. Action 7 is a review timer only. Any native-manager, entry,
sizing, or roster change still requires a separate operator approval and a new
receipt-bound preview against the then-active manifest.
