# SEVE weekend decision packet · through Friday 2026-08-14

**Decision-first research packet · no automatic trading authority**

## Where we land

Monday should begin with the roster/configuration already activated after Friday's close. Do not stack another live change on top of the new `grind-v3` two-entry governor or the new `vb-ribbon-cross-iwm` first-entry-only root before either produces a forward session.

| Decision | Monday posture | Reason |
|---|---|---|
| `grind-v3` | Run the newly activated two-entry cap; 4 contracts and native manager unchanged | The cap removes repeated late entries while leaving an observable virtual path for what it blocked. Exit remains a separate question. |
| `vb-ribbon-cross-iwm` | Begin first-entry-only in Account 2 at 2 contracts | First signal is positive across the historical virtual cohort; repeated entries erase the edge. |
| `breakout-alt-v3-iwm` | Keep in Account 1 at 2 contracts | Unique but still only 2 exact scored sessions. It is the parallel IWM control. |
| `orb-ustop-ctl` | Keep Account 3 priority, entry, +50% all-out exit, and 4 contracts | Current losses generally never developed enough favorable movement for an exit to rescue them. |
| All other executing channels | Keep current entry, exit, size, and route | No additional paper behavior change clears the paired evidence and capacity gates yet. |
| Nine retired collectors | Preserve current pauses | All nine were verified already paused, reversible, and non-executing. |

## Friday result

Friday closed **10 logical trades for -$513**. Four native exits did exactly what they were supposed to do: `pb-ride` +$44, `pb-ride-itm` +$25, `grind-v3-2` +$28, and `breakout` +$24.

The loss was concentrated:

| Channel | Friday | What the path says |
|---|---:|---|
| `grind-v3` | -$320 across 3 | Entry 1 and entry 3 never developed; entry 2 reached about +19% then stopped. This is both an entry-frequency issue and a separate exit-retention issue—not one universal remedy. |
| `qqq-thrust-trail-wd` | -$124 | Reached about +27% then stopped. This is a genuine giveback candidate; keep the arm-20/keep-half shadow running. |
| `vb-macd-state` | -$120 | Reached only about +3%. Friday was entry/opportunity evidence, not proof for a manager switch. |
| `grind-smart-entries` | -$70 | Reached only about +3%. Entry quality failed to create an exit opportunity. |

The newly activated `grind-v3` cap would have blocked Friday's third -$100 trade, but it was not live during Friday's session. It would not have repaired the first two trades, which is why the exit shadow still matters independently.

## Week in one view

Latest executed configuration eras produced **-$1,112** across the week. This number is useful for desk accounting, but decisions are made from the channel rows—not the global total.

| Working controls | Week result | Interpretation |
|---|---:|---|
| `momo-shape-2` | +$120; 2/2 | Native behavior remains a useful untouched control. |
| `qqq-thrust-trail-wd` | +$78; 1/2 | One strong win and one large giveback loss; keep the paired exit shadow. |
| `breakout-alt-v3-itm` | +$76; 1/1 | Promising, still one session. |
| `grind-v3-2` | +$60; 2/2 | Positive control, still early. |
| `breakout` | +$52; 2/2 | Native target is currently capturing reachable moves. |
| `vb-macd-state` | +$32; 2/3 | Positive total with one fresh no-opportunity loss; manager evidence remains mixed/early. |

| Channels requiring focused diagnosis | Week result | Primary question |
|---|---:|---|
| `orb-ustop-ctl` | -$496; 1/5 | Why did four current-era entries fail to develop? |
| `grind-v3` | -$496; 1/6 | How much does the two-entry governor protect, and can a separate exit shadow retain entry-2 gains? |
| `pb-ride` | -$212; 1/3 | Which entry-quality variable distinguishes the target-reaching Friday trade from the two failures? |
| `orb-qqq-trail` | -$110; 0/1 | Collect more complete entry/path evidence; one loss is not a trail verdict. |
| `vb-gap-drift` | -$108; 0/1 | Exact-current sample is one trade; do not pool it with older configurations. |

## ORB conclusion

The active `orb-ustop-ctl` signal implementation did not change during the observed swing; the manager/configuration era did. In its current +50 all-out era, four of five fills never exceeded roughly +11% favorable movement. LOCK50/30, LOCK30/30, BANK20/RUN50, and other tested managers all remained losses on the same five fills.

The most useful next ORB shadow is therefore an **entry-quality test**, not an exit replacement. Relative volume is the strongest descriptive lead: versioned historical fills above 2.5 relative volume performed worse than lower buckets. That is a candidate upper-bound shadow, not a live gate. Time of day does not support a broad delay.

## Exit conclusions

Giveback is important, but it is not the same problem on every channel.

- **Reachable move, poor retention:** `qqq-thrust-trail-wd` Friday and the middle `grind-v3` entry merit paired exit work.
- **No meaningful move:** Friday's `vb-macd-state`, `grind-smart-entries`, and two of three `grind-v3` trades are entry-quality evidence.
- **Native captured the move:** `pb-ride`, `pb-ride-itm`, `grind-v3-2`, and `breakout` should not be changed because other channels gave back profit.
- **Historical dark exit candidates:** `fomc-follow` has the strongest stable virtual manager plateau; retired predecessor `momo-shape` has a weaker, outlier-sensitive shadow case. Neither is a live-root manager instruction.

No live manager switch is recommended before Monday.

## IWM conclusion

The two Monday IWM roots currently look complementary enough to test together. Their collision graph has zero same-clock and zero same-OCC overlap in the small comparable cohort. `vb-ribbon-cross-iwm` must remain first-entry-only; the all-entry version falls from a +$14.50 typical session and 20/27 positive sessions to a -$1.70 typical session and 13/27 positive sessions.

Do not add a third IWM root Monday. The best research leads are `vb-curl-reversal-iwm`, `vb-rsi-revert-iwm`, and `vb-or-fail-iwm`, each with a channel-specific entry-frequency question. None clears full-history/session-level promotion review yet.

## Capacity and sizing

No additional contract is supported this weekend. The portfolio replay found no stable next-contract benefit after displacement, and the two new Monday experiments need attribution. Re-run capacity after the first forward week using actual deployment frequency and displaced opportunities. This is not a permanent size ceiling.

## Active research queue

| Lane | Channels | Action |
|---|---|---|
| Entry-frequency shadows | `breakout-alt-v3`, `breakout-smart-entries-ctl`, `vb-curl-reversal` | Continue registered one-variable tests. |
| Current-root trail shadows | `vb-macd-state`, `momo-shape-2`, `pb-ride` | Continue; none is paper-switch ready. |
| Strong dark exit research | `fomc-follow`, `momo-shape` predecessor | Freeze bounded challenger definitions; do not alter live roots. |
| Retirement | 9 verified paused collectors | Preserve history and receipts; resume only for a new unique hypothesis. |

## What remains this weekend

1. Freeze the exact Monday scorecard for `grind-v3`, both IWM roots, ORB priority, and every displaced opportunity.
2. Define the ORB relative-volume upper-bound shadow without changing the live entry.
3. Register the two dark exit challengers as research-only comparisons if their exact native policies can be stamped.
4. Reconcile dashboard wording so newly activated configurations correctly show **zero forward sessions** while historical virtual evidence remains visible separately.
5. Fix two presentation defects in the generated research output: the weekly virtual section can render empty despite retaining 49 rows, and capacity currently exposes account UUIDs instead of account names.
6. Run deterministic selftests and diff checks on any research/UI fixes; no merge, deployment, or activation follows without approval.

## Trust boundary

- Executed results use logical trades and exact configuration eras.
- Historical virtual results remain labeled as unstamped research.
- Manager results are same-opportunity counterfactuals, not executed P&L.
- Post-close activations have zero forward trading sessions and are not back-credited with old results.
- Cross-account same-OCC remains permitted; same-account admission and displacement remain explicit.
- The production preflight was SELECT/GET-only: zero broker writes, zero production writes, zero manifest change.
