{
  "//": "Worker typecheck config. Targets Node (not the DOM) and pulls in the engine/* + lib/* files it imports via relative paths (../engine, ../lib) so drift is caught. The ROOT tsconfig EXCLUDES `worker` (see ../tsconfig.json), so this never affects the Next build.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "types": ["node"],
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.ts"]
}
