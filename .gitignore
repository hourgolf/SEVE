# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env — never commit real keys
.env
.env.local
.env*.local

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# local editor / agent tooling (not part of the app)
.claude/

# Databento research cache (transient, local-only — never warehoused in Supabase)
data/

# user reference artifacts (not app source) — keep local, don't track
TR-909_T_600_FNL_A.jpg
Trading Thesis/files.zip
